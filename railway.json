{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["node"],
      "variables": {
        "NODE_VERSION": "20"
      }
    }
  },
  "deploy": {
    "startCommand": "npm run start",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 100,
    "postDeploy": "npm run create-test-user || echo 'Test user already exists or script skipped'"
  }
}
